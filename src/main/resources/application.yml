

server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ipam
    username: ipam
    password: ipam

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379

logging:
  level:
    root: INFO
